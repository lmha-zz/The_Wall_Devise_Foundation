<li class="cta-button">
  <div class="small-11 small-offset-1 columns">
    <h5>Post a Comment</h5>
    <%= form_for([message, @comment]) do |c| %>
      <%= c.text_area :comment %>
      <%= c.submit 'Post a Comment', class: 'right button tiny' %>
    <% end %>
  </div>
</li>